let score ='100';

if (score >= 80) {
  console.log(`${score}は80点以上なので合格です`)
}if (int <=79 && int >=50) {
  console.log(`${score}は50点以上なので惜しいです`);
}if (int <=49 && int >=30) {
  console.log(`${score}は30点以上なので残念です`);
}